门面模式
	场景：给人写信，写好内容，放到信封，投递到信箱邮递，自己去做累死了，所以有邮局，给邮局就行
	要素：信接口，信实现，调用类
	实现：LetterProcess，LetterProcessImpl，Client
	---实现方式缺陷，调用方必须知道哪些接口，必须知道调用顺序，没有高聚合
	解决方式：
	增加一个门面，Client调用的时候调用门面就好，不用了解具体的实现以及相关的业务，增加类ModenPostoffice